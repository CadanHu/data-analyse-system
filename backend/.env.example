# DeepSeek API 配置
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# 安全配置
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7  # 建议生成一个强随机字符串
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # Token 过期时间，单位分钟，默认 7 天

# Agent 配置
# USE_LANGCHAIN=true  # 启用 LangChain SQL Agent（默认 false，使用原生实现）

# 会话数据库配置 (是否使用 MySQL 存储用户会话)
USE_MYSQL_FOR_SESSIONS=false
MYSQL_SESSION_DATABASE=data_pulse_sessions

# 业务 MySQL 数据库配置（可选）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=test
MYSQL_USER=root
MYSQL_PASSWORD=root

# 注意：
# - 复制此文件为 .env
# - 将 your-deepseek-api-key-here 替换为你的实际 DeepSeek API Key
# - SECRET_KEY 必须在生产环境中更换，可以使用 `openssl rand -hex 32` 生成
# - 如果 USE_MYSQL_FOR_SESSIONS=true，则需要提供正确的 MySQL 连接信息
# - MySQL 配置用于连接 MySQL 数据库，填写后在系统中可选择 MySQL 类型的数据库
